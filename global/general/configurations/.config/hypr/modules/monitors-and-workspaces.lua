----------------
--- MONITORS ---
----------------

-- Fallback rule to mirror the main monitor by default
hl.monitor({
    output   = "",
    mode     = "preferred",
    position = "auto",
    scale    = "auto",
})
